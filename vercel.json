{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "build": {
    "env": {
      "VITE_API_URL": ""
    }
  },
  "rewrites": [
    { "source": "/api/lms/:path*", "destination": "/api/backend" },
    { "source": "/:path*",         "destination": "/index.html"   }
  ]
}
